{% extends 'base.html.twig' %}

{% block title %}New Job{% endblock %}

{% block body %}
    <h1 class="mainTitle">Publier une offre</h1>
    {% if is_granted('ROLE_EMPLOYER') %}
        {{ include('job/_form.html.twig') }}
    {% else %}
        <p class="text-center">Seuls les recruteurs peuvent accéder à cette page
            <a href="{{ path('app_login') }}">Connexion</a>
        </p>

    {% endif %}
    <div class="center">
        <a href="{{ path('job') }}">Retour</a>
    </div>
{% endblock %}
